import styled from "styled-components";

export const FooterContainer = styled.footer`
  margin-top: 0.75rem;
  display: flex;
  justify-content: space-evenly;
  align-items: start;
  padding-top: 0.5rem;
  height: 6.25rem;
  width: 100%;
  background-image: linear-gradient(
    to left bottom,
    var(--magenta),
    var(--purple),
    var(--blue-purple),
    var(--deep-blue),
    var(--deep-blue-secondary)
  );
`;
